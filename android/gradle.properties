org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Projekt (F:) und Pub-Cache (C:) liegen auf verschiedenen Laufwerken — die
# inkrementelle Kotlin-Kompilierung kann dann keinen relativen Pfad bilden
# ("different roots") und bricht ab. Deaktivieren behebt den Release-Build.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
